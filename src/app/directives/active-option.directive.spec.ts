import { ActiveOptionDirective } from './active-option.directive';

describe('ActiveOptionDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveOptionDirective();
    expect(directive).toBeTruthy();
  });
});
